---
name: Bump Alpine version

scms:
  default:
    kind: github
    spec:
      user: "{{ .github.user }}"
      email: "{{ .github.email }}"
      owner: "{{ .github.owner }}"
      repository: "{{ .github.repository }}"
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
      branch: "{{ .github.branch }}"

sources:
  latestVersion:
    kind: githubrelease
    name: "Get the latest Alpine Linux version"
    spec:
      owner: "alpinelinux"
      repository: "aports" # Its release process follows Alpine's
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
      versionfilter:
        kind: semver
        pattern: "~3"
    transformers:
      - trimprefix: "v"
  latestVersionNoPatch:
    kind: shell
    name: Latest Alpijne Version without the patch part
    dependson:
      - latestVersion
    spec:
      command: echo {{ source "latestVersion" }}
    transformers:
      - findsubmatch:
          pattern: '(\d*).(\d*).\d*'
          capturepattern: '\1.\2'

conditions:
  testDockerfileArg:
    name: "Does the Dockerfile have an ARG instruction for the Alpine Linux version?"
    kind: dockerfile
    disablesourceinput: true
    spec:
      file: alpine/Dockerfile
      instruction:
        keyword: "ARG"
        matcher: "ALPINE_TAG"
  testDockerImageExists:
    name: "Does the Docker Image exist on the Docker Hub?"
    kind: dockerimage
    sourceid: latestVersion
    spec:
      image: "alpine"
      # tag come from the source
      architecture: amd64

targets:
  updateDockerfile:
    name: "Update the value of the base image (ARG ALPINE_TAG) in the Dockerfile"
    kind: dockerfile
    sourceid: latestVersion
    spec:
      file: alpine/Dockerfile
      instruction:
        keyword: "ARG"
        matcher: "ALPINE_TAG"
    scmid: default
  updateDockerBake:
    name: "Update the value of the base image (ARG ALPINE_TAG) in the docker-bake.hcl"
    kind: hcl
    sourceid: latestVersion
    spec:
      file: docker-bake.hcl
      path: variable.ALPINE_FULL_TAG.default
    scmid: default
  updateGoldenTags:
    name: Update Alpine image tags in the golden tags system
    kind: file
    sourceid: latestVersionNoPatch
    spec:
      file: tests/golden/expected_tags.txt
      matchpattern: '(.*)alpine(\d.).(\d*)(.*)'
      replacepattern: '${1}alpine{{ source "latestVersionNoPatch" }}${4}'
    scmid: default
actions:
  default:
    kind: github/pullrequest
    scmid: default
    title: Bump Alpine Linux Version to {{ source "latestVersion" }}
    spec:
      labels:
        - dependencies
